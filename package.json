{
  "name": "seidel",
  "version": "0.0.0",
  "description": "Seidel's polygon triangulation algorithm implementation",
  "main": "src/seidel.js",
  "scripts": {
    "test": "jshint src",
    "build-dev": "mkdir -p dist && browserify -d src/seidel.js -s seidel > dist/seidel.dev.js",
    "build-min": "mkdir -p dist && browserify src/seidel.js -s seidel | uglifyjs -c warnings=false -m > dist/seidel.min.js"
  },
  "author": "Mason Green",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^5.9.3",
    "jshint": "^2.5.3",
    "mkdirp": "^0.5.0",
    "uglifyjs": "^2.3.6"
  },
  "jshintConfig": {
    "node": true,
    "unused": true
  }
}
